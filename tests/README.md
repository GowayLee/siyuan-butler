# Butler Tests

This directory is reserved for future verification of the TypeScript Butler runtime.

Planned structure:

- `unit/`
- `integration/`
- `fixtures/`

Current focus:

- mocked SiYuan HTTP-response tests for Butler MCP runtime flows
- no live SiYuan instance required for routine verification

Run with:

- `npm test`
