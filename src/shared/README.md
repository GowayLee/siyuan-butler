# shared

Planned home for shared runtime helpers, schemas, constants, and small cross-cutting modules.

Keep this directory focused on reusable support code rather than business workflow orchestration.
