# domain

Planned home for Butler's stable domain model and policy-sensitive rules.

Expected focus areas include `SparkleDraft`, `RekindleRequest`, `RekindleProposal`, `WritePlan`, and `ReviewResult`.
