# Butler Runtime Source

This directory is reserved for the future TypeScript implementation of the SiYuan Butler runtime.

Planned major areas:

- `butler-mcp/` - Butler MCP server runtime
- `domain/` - Butler domain models and policy-oriented rules
- `adapter/` - low-level SiYuan integration and other external adapters
- `shared/` - shared schemas and utilities
