# adapter

Planned home for low-level adapters used by the Butler runtime.

This includes the future SiYuan integration layer that Butler will own directly, instead of relying on `vendor/` as the primary implementation.
